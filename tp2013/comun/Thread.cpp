/*#include "Thread.h"
 
int Thread::iniciarThread() {
    return pthread_create(&ptrThread, NULL, accionThread, this);
}

pthread_t Thread::getPtrThread() {
    return ptrThread;
}
    
void Thread::finalizarThread() {
	pthread_join(ptrThread, NULL);
}
*/
